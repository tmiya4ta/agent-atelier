<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <!-- mule.env を CLI/環境変数で上書き可能にする (default = local) -->
    <global-property name="mule.env" value="local"/>
    <configuration-properties file="config/config-${mule.env}.yaml"/>

    <!-- HTTP listener (inbound = この Mule Agent の A2A エンドポイント) -->
    <http:listener-config name="http-listener-config">
        <http:listener-connection host="0.0.0.0" port="${http.port}"/>
    </http:listener-config>

    <!-- HTTP requester (outbound = ft0 Flex Gateway 経由で Python A2A(snowapp) へ)。
         url は都度フル指定するが、 TLS/SNI 用に upstream host を connection に置く。 -->
    <http:request-config name="agent-request-config">
        <http:request-connection protocol="HTTPS" host="${upstream.host}" port="443"/>
    </http:request-config>

</mule>
